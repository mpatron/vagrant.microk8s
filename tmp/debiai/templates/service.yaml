apiVersion: v1
kind: Service
metadata:
  name: {{ include "debiai.fullname" . }}
  labels:
    {{- include "debiai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "debiai.selectorLabels" . | nindent 4 }}
