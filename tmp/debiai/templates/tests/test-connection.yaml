apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "debiai.fullname" . }}-test-connection"
  labels:
    {{- include "debiai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "debiai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
